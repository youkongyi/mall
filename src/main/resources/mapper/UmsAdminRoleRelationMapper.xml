<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.youkongyi.mall.mapper.UmsAdminRoleRelationMapper">
    <select id="getPermissionList" resultType="com.youkongyi.mall.model.UmsPermission">
        SELECT p.id, p.pid, p.name, p.value, p.icon, p.type, p.uri,
               p.status, p.create_time, p.sort
        FROM ums_admin_role_relation ar
                 LEFT JOIN ums_role r ON ar.role_id = r.id
                 LEFT JOIN ums_role_permission_relation rp ON r.id = rp.role_id
                 LEFT JOIN ums_permission p ON rp.permission_id = p.id
        WHERE ar.admin_id = #{adminId}
          AND p.id IS NOT NULL
          AND p.id NOT IN (SELECT p.id
                           FROM ums_admin_permission_relation pr
                                    LEFT JOIN ums_permission p ON pr.permission_id = p.id
                           WHERE pr.type = - 1
                             AND pr.admin_id = #{adminId})
        UNION
        SELECT p.id, p.pid, p.name, p.value, p.icon, p.type, p.uri,
               p.status, p.create_time, p.sort
        FROM ums_admin_permission_relation pr
                 LEFT JOIN ums_permission p ON pr.permission_id = p.id
        WHERE pr.type = 1
          AND pr.admin_id = #{adminId}
    </select>

    <select id="getRoleList" resultType="com.youkongyi.mall.model.UmsRole">
        SELECT
            t2.id,
            t2.`name`,
            t2.description,
            t2.admin_count as adminCount,
            t2.create_time as createTime,
            t2.`status`,
            t2.sort
        FROM
            ums_admin_role_relation  t
        INNER JOIN ums_role  t2
           ON t.role_id = t2.id
        WHERE
            t.admin_id = #{adminId}
    </select>
</mapper>